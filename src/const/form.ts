export interface FormFieldData {
  name: string;
  label: string;
  type: string;
  placeholder?: string;
  required?: boolean;
}

export interface FormFieldDataItem {
  id: string;
  name: string;
}
